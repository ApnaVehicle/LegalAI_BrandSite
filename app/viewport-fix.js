// Viewport configuration for Next.js 13+ App Router
export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
  viewportFit: 'cover',
}

export const metadata = {
  // Additional metadata if needed
  formatDetection: {
    telephone: false,
  },
}